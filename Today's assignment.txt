9/24/19

Today's assignment:

In your file from last class,
1) Make sure to finish the last assignment.
2) For that file, create and link a css file
3) In the css, using tagnames, classes, ids (3+ of each)
	a) tagname (e.g. h1, p, ol, ul)
	b) class (e.g. .my_class1, .my_class2)
	c) id (e.g. #myId, #AnotherID)
4) In your css file, change text colors, resize photos, font style
5) Once finished, shtup your files on github
